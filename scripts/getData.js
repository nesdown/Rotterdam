'use strict';
let data = '';
  //data = String($('input').val());

setTimeout(
  function {
    $('#output').data('Waiting for input.');
  },
  1000
);

setTimeout(
  function {
    $('#output').data('Waiting for input..');
  },
  1000
);

setTimeout(
  function {
    $('#output').data('Waiting for input...');
  },
  1000
);

setTimeout(
  function {
    $('#output').data('Waiting for input..');
  },
  1000
);

setTimeout(
  function {
    $('#output').data('Waiting for input.');
  },
  1000
);
